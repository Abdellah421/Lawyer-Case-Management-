import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lawyer.casemanagementpro',
  appName: 'دفتر القضايا',
  webDir: 'dist'
};

export default config;
